using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Toaster : MonoBehaviour
{
    public static GameObject toasty;

    // Start is called before the first frame update
    void Start()
    {
        toasty = gameObject;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
